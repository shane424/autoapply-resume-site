# CLAUDE.local.md — Local Machine Overrides

> Personal overrides for this project on THIS machine only.
> This file is gitignored — never committed. Safe for local paths, dev credentials, personal shortcuts.

---

## Local Dev URLs

```
Local server:     http://localhost:[PORT]
Admin panel:      http://localhost:[PORT]/admin
API base:         http://localhost:[PORT]/api
DB connection:    [FILL IN]
```

---

## Local Credentials / Test Data

<!-- Never commit real credentials. This file is gitignored. -->

Test user email: [FILL IN]
Test user password: [FILL IN]
Test API key (dev only): [FILL IN]

---

## Machine-Specific Paths

```
Project root:     [FILL IN]
Assets folder:    [FILL IN]
ComfyUI install:  [FILL IN — e.g. ~/ComfyUI]
SD models:        [FILL IN — e.g. ~/ComfyUI/models/checkpoints]
Godot binary:     [FILL IN — e.g. /Applications/Godot.app]
```

---

## Personal Dev Preferences (this project only)

<!-- Overrides that only make sense for your setup -->

- [e.g. "Use port 3001 — 3000 is taken by another project"]
- [e.g. "Skip email verification in dev — use the test bypass route"]
- [e.g. "My local DB is named closer_dev not closer"]

---

## Local Bash Shortcuts

<!-- Commands Claude can run on this machine -->

```bash
# Start everything
[FILL IN — e.g. npm run dev:all]

# Reset dev DB
[FILL IN]

# Run specific test suite
[FILL IN]
```
